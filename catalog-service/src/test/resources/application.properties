# ? Completely disable Spring Cloud Config during tests
spring.cloud.config.enabled=false

# ? Force Spring to use only local properties (no config server)
spring.config.import=optional:classpath:/application.properties

# Optional: make sure the test doesn't fail fast
spring.cloud.config.fail-fast=false
